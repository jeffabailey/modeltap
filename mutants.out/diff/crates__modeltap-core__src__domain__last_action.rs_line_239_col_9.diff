--- crates/modeltap-core/src/domain/last_action.rs
+++ replace LastAction::for_delete_one_failed -> Self with Default::default()
@@ -231,18 +231,11 @@
             bytes_retained: 0,
             extra,
         }
     }
 
     /// Construct a failure banner for a delete-from-one that errored before
     /// any work (or where the plugin reported NotFound).
     pub fn for_delete_one_failed(target: String) -> Self {
-        Self {
-            verb: ActionVerb::Delete,
-            target,
-            status: ActionStatus::Failed,
-            bytes_reclaimed: 0,
-            bytes_retained: 0,
-            extra: None,
-        }
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 }
