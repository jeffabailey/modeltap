--- crates/modeltap-core/src/domain/last_action.rs
+++ replace LastAction::for_unify_failed -> Self with Default::default()
@@ -194,24 +194,17 @@
             bytes_retained: 0,
             extra: None,
         }
     }
 
     /// Construct a failure banner for a unify that errored before any work
     /// (or where every target failed).
     pub fn for_unify_failed(target: String) -> Self {
-        Self {
-            verb: ActionVerb::Unify,
-            target,
-            status: ActionStatus::Failed,
-            bytes_reclaimed: 0,
-            bytes_retained: 0,
-            extra: None,
-        }
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     /// Construct a success banner for a confirmed single-model delete-from-one
     /// (US-05b, step 03-06; ADR-009). `target` is the model id (NOT a tool
     /// id — delete-from-one acts on a single model). `bytes_reclaimed` is
     /// the bytes freed by the unlink (zero when the blob was shared and
     /// kept on purpose). `was_shared` is the orchestrator's classification;
     /// when true and `bytes_reclaimed == 0`, the body line includes
