// modeltap-acceptance — black-box acceptance test crate.
//
// All real test code lives under `tests/acceptance/**`. This `lib.rs` exists
// only so Cargo treats this directory as a library crate; it has no public
// API. Acceptance tests are integration tests at this crate's `tests/` dir.
